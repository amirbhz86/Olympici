// import { useDataLoader,  } from '../../../hooks/index'
// import { ContainerInput } from '../models'

const useContainer = (containerInput: any ) => {
  const { navigation } = containerInput
  // const { userType } = useDataLoader({ navigation })
  // const { Get } = useRedux()
  // const { currentStack } = Get().app

  return {
    // userType,
  }
}

export default useContainer
