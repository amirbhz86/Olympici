import { StyleProp, ViewStyle } from "react-native";

export interface Props {
  normString?: string,
  pressString?: string,
  buttonString?: string,
  style?: object,
  onPress?: any,
  buttonOnPress ?: any,
  textOnPress ?: any,
}
